# Передача native Terra

Рабочий источник: `terra-full-v1/assembled/manuscript.md`, SHA-256 `ab11580a6c9adcaa9a3af4696a4466979e38f6e138633e5230f802d675ff1c6b`. Источник неизменяем. Написать окончательные литературные замены самостоятельно, по `PATCH-PLAN.json`; новых художественных абзацев Astra не предлагает.

1. Прочитать полный источник, весь план, реестр и полный outline/state, включая no-change guards. Для каждого из 27 patches вернуть точный исходный `before` и свой `after`, сохраняя ID. Все исходные абзацы уникальны в данном SHA. Номера P относятся к непустым блокам сборки, включая заголовки, а не к физическим строкам language_qa.
2. Соблюсти зависимости: техническое поле CH22 → ограниченный вывод CH23; реальные обращения CH29 → доступные выписки CH31. Не раскрывать числовое сравнение в CH29. Условия возвращения нужны для реплики «Розклад», но поездки на грузовике не было.
3. Для 22 `metadata_updates` использовать настоящие `scene_id`, `field`, точное `before`. Поля — time_basis/events/knowledge/resources. Для зависимых новых событий сначала написать прозу, затем дать короткие буквальные подтверждения и окончательный observed state. Не превращать предложенные часы в наблюдённые. `global_registry_updates` исполняет координатор отдельно, это не поле сцены.
4. Не менять optional/context-preserved пункты как будто их принял автор. Реестр хранит pending индивидуальных решений; совместимый пакет разрешён общим поручением. Не добавлять канон и не закрывать остальные unknowns.
5. Сохранить реальные промахи, различие версии до события и подстройки после него, закрытый личный просмотр и автономию Оси. Новая функциональность Баласта действует только в данном конечном мандате; прежние home/mic OFF не отменяются.
6. Сохранить шутки в обеих репликах, самостоятельную работу Тараса, поправленный payoff рубанка, отказ от второго билета, нечтение оставшихся будущих страниц и обычное последнее решение о хлебе. Новый номер запроса сохраняется. Никакой тайной формулы бюджета, всеобщего запрета прав, предопределённой многонедельной цепи или реткона восстания.

После применения координатор создаёт новую версию, перепривязывает 41 observed scene и цитаты к новым SHA, проверяет все 12 no-change items и восемь рабочих unknowns. Новая Astra-проверка должна прочитать весь итоговый текст, а не только изменённые абзацы. Первоначальный source, отзывы и этот диагностический пакет сохраняются неизменными.
