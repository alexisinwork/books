# Проверки draft-v2

Проверенный target: `target.uk.txt`, SHA-256 `703b831984127e59061e6a87fd72728ecdb3097f84ff235cf59691a3347e4a8e`; полный рассказ, 236 целевых абзацев, 8722 лексических токена штатного QA, 9160 слов по пробелам. Источник: p1–p248, 236 непустых ID.

`literary_translation.py check`: `mechanically_checked`, 0 errors, 236/236 source ID и 236 target paragraphs. Все ID уникальны, идут от `p000001` до `p000248` в порядке frozen packet. 68 предупреждений `unresolved_term` ожидаемы для переданного базового глоссария со статусами `proposed`; базовый файл не изменён. Отчёт `literary-translation-check.json`, SHA-256 `fafcfc9d493a824a6d83f9cc3be95a528dba5c9e1948b0493dc9439276c98d0f`.

`language_qa.py --language uk`: `no_configured_signals`, находок нет. Отчёт `language-qa.json`, SHA-256 `1e4481daa26a6ffa4207e91ce5e4c91f120ea2e2b04098cf16fe92b88a1b05d5`.

Сравнение с `draft-v1`: изменены ровно 12 source ID, перечисленных в `changes.json`; остальные 224 целевых блока совпадают. Три `target_emphasis` p167, p209 и p234 дословно входят в соответствующие target paragraphs и не изменены. Семь `chunks` синхронизированы с итоговым `target.uk.txt` и `translation.json`.

Проверено чтением: три формулы с `ляск` полностью одинаковы в p5/p66 и совпадают по рамке с сокращённым финалом p248; p120 по-прежнему впервые раскрывает промах; Джулія/Крістіна раскрывается в исходной точке; противоречия признания Лайта и неназванный результат финального выстрела сохранены. F01–F03 и F10 не получили новой информации. Это редакторский self-check, не новая независимая модельная рецензия.

Старые DOCX/PDF, рендеры и проверки `draft-v1` в `draft-v2` не переносились: новый reader должен собираться только от финального SHA этой версии.
