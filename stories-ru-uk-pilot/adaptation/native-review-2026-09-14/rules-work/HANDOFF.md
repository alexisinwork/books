Подготовлен кандидат общих правил и инструментов; общие файлы мастерской и проза этим заданием не изменялись. Точные основы, новые SHA и шесть путей для переноса находятся в [MANIFEST.json](MANIFEST.json); прочитанная база сохранена в `base/`, новые файлы — в `candidate/`, единый diff — [candidate.patch](candidate.patch).

Кандидат сохраняет `source_units`, старые `p/start/quote`, резолюции и поведение `--require-reviewed`. Оба сканера отдельно показывают пустые/непустые единицы и отличают строку TXT от абзаца DOCX. `reading_coverage` остаётся `not_assessed_by_this_tool` с `null` вместо выдуманного количества прочитанного. В трёх базовых UK-файлах наблюдаются 58, 236 и 310 непустых единиц, всего 604. Исторические 600 RU ID — другой знаменатель.

В NATURALNESS, STYLE и навыке уточнены фактический охват, смысловая точность действия, повторное объяснение эмоции и функция разговорной реплики. Новый узкий сигнал `pupils_as_gaze_instrument` находит «глянувши … своїми … зіницями», требует контекстного решения и допускает мотивированное сохранение. Обычные «погляди зосередилися», «дичина», «щирість», описания глаз и метонимия не стали автоматическими ошибками. Основания, точные цитаты, границы и контрпримеры — [rule-evidence.json](rule-evidence.json). Новых нормативных запретов и авторских эталонов нет; OLEK_UA_STYLE не менялся.

Проверено: 20 поведенческих тестов естественности/охвата, 3 существующие языковые регрессии, валидация навыка и применение patch в режиме `--check` к сохранённой базе. Команды и SHA проверенных файлов — [VALIDATION.json](VALIDATION.json). Повторное механическое сканирование базовых рассказов даёт 1/0/0 контекстных сигналов; оно не удостоверяет прочтение или качество. Его отчёты сохранены в `validation-v1/`.

При переносе сравнить текущие общие файлы с базовыми SHA. Чужие параллельные изменения уже были обнаружены между TASK-снимком и базой этого кандидата; они сохранены в кандидатах. Если текущий файл снова отличается, свести конкретные изменения с его содержимым. Изменения двух инструментов переносить вместе: `uk_naturalness.py` импортирует новый `source_coverage` из `language_qa.py`. `git apply --check` выполнялся только на сохранённой базе и не удостоверяет отсутствие конфликтов в текущем корне.

Проза первого базового рассказа прочитана в этой задаче для проверки примеров правил, с уже известными предложениями родительского редактора. Это не независимый слепой отчёт и не проверка новой редакции. Второй и третий рассказы здесь только механически сканировались; полное чтение выполняют назначенные редакторы.
